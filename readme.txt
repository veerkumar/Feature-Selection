1) Start_face and start_EEG are the script for each dataset
2) start_foot script for running footdataset.

In both the script, remove "for j=1:3" loop if you want to run for only 2 iteration(or modify loop_len variable to increse).
 This loop used to run the script for 10,50 & 100 iteration. 